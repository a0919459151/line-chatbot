const groupDao = require('../mongodb/groupDao')
const userDao = require('../mongodb/userDao')
const scheduleDao = require('../mongodb/scheduleDao')

module.exports = {
  groupDao,
  userDao,
  scheduleDao
}
